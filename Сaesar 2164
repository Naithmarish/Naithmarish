s = input()
n = int(input())
alpha = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
s = s.upper()
res = " "

for c in s:
    old = alpha.find(c)
    new = old - n
    if c in alpha:
        res += alpha[new]
    else:
        res += c
        
print(res.lstrip())
